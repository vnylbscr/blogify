import { GET_POSTS } from "../actions/posts";

const initialState = {
  posts: null,
};

export const postReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      break;
  }
};
